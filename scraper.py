import aiohttp\nimport asyncio\nimport requests\nfrom bs4 import BeautifulSoup\nimport time\nfrom urllib.parse import urlparse\n\nclass RateLimiter:\n    def __init__(self):\n        self.last_request_time = {}\n        self.min_delay = 1\n\n    def wait(self, url):\n        domain = urlparse(url).netloc\n        current_time = time.time()\n        if domain in self.last_request_time:\n            elapsed_time = current_time - self.last_request_time[domain]\n            if elapsed_time < self.min_delay:\n                time.sleep(self.min_delay - elapsed_time)\n        self.last_request_time[domain] = time.time()\n\nasync def fetch(session, url):\n    rate_limiter.wait(url)\n    async with session.get(url) as response:\n        return await response.text()\n\ndef extract_links(html, domain_filter=None):\n    soup = BeautifulSoup(html, 'html.parser')\n    links = [a['href'] for a in soup.find_all('a', href=True)]\n    if domain_filter:\n        links = [link for link in links if domain_filter in link]\n    return links\n\nasync def scrape(urls, domain_filter=None):\n    async with aiohttp.ClientSession() as session:\n        tasks = [fetch(session, url) for url in urls]\n        responses = await asyncio.gather(*tasks)\n        return [extract_links(html, domain_filter) for html in responses]\n\nif __name__ == '__main__':\n    urls = ['http://example.com']\n    loop = asyncio.get_event_loop()\n    links = loop.run_until_complete(scrape(urls, domain_filter='example.com'))\n    print(links)