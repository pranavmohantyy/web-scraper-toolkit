import aiohttp\nimport asyncio\nimport requests\nfrom bs4 import BeautifulSoup\nimport time\nimport os\nfrom urllib.parse import urlparse\n\nclass RateLimiter:\n    def __init__(self):\n        self.last_request_time = {}\n        self.min_delay = 1\n\n    def wait(self, url):\n        domain = urlparse(url).netloc\n        current_time = time.time()\n        if domain in self.last_request_time:\n            elapsed_time = current_time - self.last_request_time[domain]\n            if elapsed_time < self.min_delay:\n                time.sleep(self.min_delay - elapsed_time)\n        self.last_request_time[domain] = time.time()\n\nasync def download_image(session, url, folder):\n    if not os.path.exists(folder):\n        os.makedirs(folder)\n    async with session.get(url) as response:\n        if response.status == 200:\n            image_data = await response.read()\n            image_name = os.path.join(folder, url.split('/')[-1])\n            with open(image_name, 'wb') as f:\n                f.write(image_data)\n\nasync def scrape_images(url, folder):\n    async with aiohttp.ClientSession() as session:\n        async with session.get(url) as response:\n            if response.status == 200:\n                html = await response.text()\n                soup = BeautifulSoup(html, 'html.parser')\n                img_tags = soup.find_all('img')\n                tasks = [download_image(session, img['src'], folder) for img in img_tags if 'src' in img.attrs]\n                await asyncio.gather(*tasks)\n\n# Example usage\n# asyncio.run(scrape_images('http://example.com', 'images'))\n