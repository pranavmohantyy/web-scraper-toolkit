import aiohttp\nimport asyncio\nimport requests\nfrom bs4 import BeautifulSoup\nimport time\nimport os\nfrom urllib.parse import urlparse\n\nclass RateLimiter:\n    def __init__(self):\n        self.last_request_time = {}\n        self.min_delay = 1\n\n    def wait(self, url):\n        domain = urlparse(url).netloc\n        current_time = time.time()\n        if domain in self.last_request_time:\n            elapsed_time = current_time - self.last_request_time[domain]\n            if elapsed_time < self.min_delay:\n                time.sleep(self.min_delay - elapsed_time)\n        self.last_request_time[domain] = time.time()\n\nasync def fetch(url, session, rate_limiter):\n    rate_limiter.wait(url)\n    async with session.get(url) as response:\n        return await response.text()\n\nasync def scrape_wikipedia(topic):\n    url = f'https://en.wikipedia.org/wiki/{topic}'\n    rate_limiter = RateLimiter()\n    async with aiohttp.ClientSession() as session:\n        html = await fetch(url, session, rate_limiter)\n        soup = BeautifulSoup(html, 'html.parser')\n        summary = soup.find('div', class_='mw-parser-output').find('p').text\n        sections = [h2.text.strip() for h2 in soup.find_all('h2')]\n        internal_links = [a['href'] for a in soup.find_all('a', href=True) if a['href'].startswith('/wiki/') and ':' not in a['href']]\n        return {'summary': summary, 'sections': sections, 'internal_links': internal_links}\n\ndef main():\n    topic = 'Python_(programming_language)'\n    loop = asyncio.get_event_loop()\n    data = loop.run_until_complete(scrape_wikipedia(topic))\n    print(data)\n\nif __name__ == '__main__':\n    main()