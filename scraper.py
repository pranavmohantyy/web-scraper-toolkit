import aiohttp\nimport asyncio\nimport requests\nimport csv\nimport json\nimport sqlite3\nfrom bs4 import BeautifulSoup\nimport time\nimport os\nfrom urllib.parse import urlparse\n\nclass RateLimiter:\n    def __init__(self):\n        self.last_request_time = {}\n        self.min_delay = 1\n\n    def wait(self, url):\n        domain = urlparse(url).netloc\n        current_time = time.time()\n        if domain in self.last_request_time:\n            elapsed_time = current_time - self.last_request_time[domain]\n            if elapsed_time < self.min_delay:\n                time.sleep(self.min_delay - elapsed_time)\n        self.last_request_time[domain] = time.time()\n\nasync def fetch(session, url):\n    async with session.get(url) as response:\n        return await response.text()\n\nasync def scrape(urls, output_format='json', db_name='results.db'):\n    rate_limiter = RateLimiter()\n    async with aiohttp.ClientSession() as session:\n        tasks = []\n        for url in urls:\n            rate_limiter.wait(url)\n            tasks.append(fetch(session, url))\n        results = await asyncio.gather(*tasks)\n        structured_data = [BeautifulSoup(result, 'html.parser').title.string for result in results]\n        save_results(structured_data, output_format, db_name)\n\ndef save_results(data, output_format, db_name):\n    if output_format == 'json':\n        with open('results.json', 'w') as json_file:\n            json.dump(data, json_file)\n    elif output_format == 'csv':\n        with open('results.csv', 'w', newline='') as csv_file:\n            writer = csv.writer(csv_file)\n            writer.writerow(['Title'])\n            for title in data:\n                writer.writerow([title])\n    elif output_format == 'sqlite':\n        conn = sqlite3.connect(db_name)\n        c = conn.cursor()\n        c.execute('CREATE TABLE IF NOT EXISTS results (title TEXT)')\n        c.executemany('INSERT INTO results (title) VALUES (?)', [(title,) for title in data])\n        conn.commit()\n        conn.close()\n\nif __name__ == '__main__':\n    urls_to_scrape = ['https://example.com', 'https://example.org']\n    asyncio.run(scrape(urls_to_scrape, output_format='json'))