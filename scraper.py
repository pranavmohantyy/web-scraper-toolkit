import aiohttp\nimport asyncio\nimport requests\nfrom bs4 import BeautifulSoup\nimport time\nimport os\nfrom urllib.parse import urlparse\n\nclass RateLimiter:\n    def __init__(self):\n        self.last_request_time = {}\n        self.min_delay = 1\n\n    def wait(self, url):\n        domain = urlparse(url).netloc\n        current_time = time.time()\n        if domain in self.last_request_time:\n            elapsed_time = current_time - self.last_request_time[domain]\n            if elapsed_time < self.min_delay:\n                time.sleep(self.min_delay - elapsed_time)\n        self.last_request_time[domain] = time.time()\n\nasync def fetch(session, url):\n    async with session.get(url) as response:\n        return await response.json()\n\nasync def scrape_hn_headlines():\n    url = 'https://hacker-news.firebaseio.com/v0/topstories.json?print=pretty'\n    async with aiohttp.ClientSession() as session:\n        headlines = await fetch(session, url)\n        return headlines\n\ndef main():\n    headlines = asyncio.run(scrape_hn_headlines())\n    print(headlines)\n\nif __name__ == '__main__':\n    main()